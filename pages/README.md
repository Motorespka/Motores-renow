# Streamlit pages (new shell)

This folder is reserved for the **incremental migration** to Streamlit's multipage navigation.

- The current stable entrypoint remains `App.py`.
- Legacy pages live in `page/` and are kept working with fallback.

