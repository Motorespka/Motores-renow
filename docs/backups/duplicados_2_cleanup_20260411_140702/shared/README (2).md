# shared/

Diretorio reservado para codigo comum entre Streamlit legado, backend FastAPI e frontend.

Uso planejado nas proximas fases:

- contratos de dados reutilizaveis
- regras de dominio puras (sem Streamlit/Next/FastAPI)
- constantes de acesso comercial (tiers/planos/roles)

Nesta etapa, o foco foi criar a base paralela sem quebrar o legado.

